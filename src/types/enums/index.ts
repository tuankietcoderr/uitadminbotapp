export enum EAuthStrategy {
  LOCAL = 'local',
  GOOGLE = 'google',
  GUEST = 'guest',
}

export enum EContentType {
  TEXT = 'text',
  IMAGE = 'image',
  DOCUMENT = 'document',
}

export enum ERole {
  SUPER_ADMIN = 'super_admin',
  ADMIN = 'admin',
  CHAT_USER = 'chat_user',
}
